{
  "name": "allright_quiz",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pw:debug": "playwright-cli close-all && playwright-cli open --browser=chrome --headed --persistent",
    "pw:clean": "node -e \"const fs=require('fs'); for (const p of ['.playwright-cli','.playwright-mcp','test-results','playwright-report']) fs.rmSync(p,{recursive:true,force:true});\"",
    "pw:fresh": "npm run pw:clean && npm run pw:debug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@faker-js/faker": "^10.6.0",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
